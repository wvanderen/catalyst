<h1>Now practicing: <%= @practice.title %></h1>

<div class='row'>
    <div class='col-md-8'>
        <p><%= @practice.description %></p>
        <p>Recommended time: <%= @practice.duration %></p>
        <p>Settle in and relax. Set a timer if necessary. Press the complete button when you're done to record the practice.</p>
    </div>
    <div class="col-md-4">
        <% if current_user %>
            <%= link_to "Complete", @practice, method: :complete, class: 'btn btn-success' %>
        <% end %>
    </div>
</div>
    

