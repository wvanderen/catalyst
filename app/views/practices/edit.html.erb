<h1>Edit Practice</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Effective Practice Writing</p>
     <ul>
       <li>Clear steps for completion.</li>
       <li>Connected to real-world effects</li>
       <li>Relevant to users.</li>
       <li>As long as they need to be but no longer</li>
       <li>Portion experience relative to difficulty and effectiveness.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @practice do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter practice title" %>
       </div>
       <div class="form-group">
         <%= f.label :description %>
         <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter practice body" %>
       </div>
        <div class="form-group">
         <%= f.label :duration %>
         <%= f.number_field :duration, class: 'form-control', placeholder: "Enter duration of practice" %>
       </div>
        <div class="form-group">
         <%= f.label :experience %>
         <%= f.number_field :experience, class: 'form-control', placeholder: "Enter experience earned from practice" %>
       </div>
       
       
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>